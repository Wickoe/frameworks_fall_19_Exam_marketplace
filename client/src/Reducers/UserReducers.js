export function users(state = {}, action) {
    switch (action) {
        case 'GET_USER':
            // TODO - missing implementation
            throw Error('Missing implementation!');
        case 'POST_USER':
            // TODO - missing implementation
            throw Error('Missing implementation!');
        default:
            return {...state}
    }
}